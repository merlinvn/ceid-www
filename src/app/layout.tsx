import type { Metadata } from "next";
import localFont from "next/font/local";
import "./globals.css";

const geistSans = localFont({
  src: "./fonts/GeistVF.woff",
  variable: "--font-geist-sans",
  weight: "100 900",
});
const geistMono = localFont({
  src: "./fonts/GeistMonoVF.woff",
  variable: "--font-geist-mono",
  weight: "100 900",
});

export const metadata: Metadata = {
  title: "CEID - Center for Computational Epidemiology and Infectious Diseases",
  description:
    "The Center for Computational Epidemiology and Infectious Diseases (CCEID) leverages advanced computational methods to combat infectious diseases globally. Our research drives innovations in epidemiology, including computational modeling, big data analytics, and machine learning, to predict disease spread and improve health outcomes. Explore our high-impact research and publications on antimalarial drug resistance, disease prevention strategies, and more.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <head>
        <link rel="icon" href="/images/logo.ico" sizes="any" />
      </head>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
