version: "3.8"

services:
  nextjs:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nextjs_frontend
    ports:
      - "3000:3000" # Map your host port to container port
    restart: always
    environment:
      - NODE_ENV=production
